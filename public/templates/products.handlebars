<h1>Products</h2>
<hr>

  {{#each products}}
    <div class="event-box" data-id="{{id}}">
      <h2>{{title}}</h2>
      <h3>{{price}}</h3>
      <h4>{{description}}</h4>
    </div>
    <hr>
  {{/each}}

